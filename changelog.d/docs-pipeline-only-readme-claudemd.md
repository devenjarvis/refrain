### Changed

- README and CLAUDE.md now lead with Refrain's purpose and unique value add — sustainable parallel-agent orchestration around the BCG/2026 3-agent ceiling, METR/2025 oversight tax, and the 90-minute block — rather than describing the dashboard as one mode among several. The pipeline view is documented as the single top-level view; the prior split-panel layout, `f` toggle, and `focus_mode_enabled` config key are no longer described as toggles.
- README has a tightened tagline, a "Why Refrain?" section that frames the design opinion before the feature list, a "Quick Start" section, an "Agent terminal" keybinding heading (replacing the user-facing `focusLaunch` jargon), and a new "Development" section covering the build/test/lint loop and changelog-fragment convention.
- CLAUDE.md "Project" section now states the design frame (≤3 agents, batch review, 90-minute block) so future contributors evaluate proposals against it. The "Dashboard" pattern bullet drops the transitional "this is the only mode — there is no toggle" framing and notes the removed `focus_mode_enabled` key for legacy-config compatibility.
