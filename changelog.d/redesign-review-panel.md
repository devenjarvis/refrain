### Changed
- Reviewing panel redesigned: tabs removed; an embedded per-task diff viewport and an inline checks strip now share one screen with the task-card ledger, eliminating the tab switches that previously fragmented the review motion.
